require 'spec_helper'

describe "test runner" do
  context "when it receives a request to run a test suite" do
    it "should call the test script depending the testing tool used"
    it "should send back the results"
    it "should run and store the code coverage and provide a link to it"
  end
end