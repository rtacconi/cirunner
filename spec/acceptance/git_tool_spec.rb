require 'spec_helper'

describe "GIT tool" do
  context "receives a request to clone a repository" do
    it "should create a directory with the ID of the project record to avoid name collision"
    it "should clone the repository the the directory of the project"
  end
end